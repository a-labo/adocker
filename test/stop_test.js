/**
 * Test case for stop.
 * Runs with mocha.
 */
'use strict'

const stop = require('../lib/commands/stop.js')
const assert = require('assert')
const co = require('co')

describe('stop', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Stop', () => co(function * () {

  }))
})

/* global describe, before, after, it */
