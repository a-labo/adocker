/**
 * Test case for hasBinAsync.
 * Runs with mocha.
 */
'use strict'

const hasBinAsync = require('../lib/helpers/has_bin_async.js')
const assert = require('assert')


describe('has-bin-async', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Has bin async', async () => {

  })
})

/* global describe, before, after, it */
