/**
 * Test case for hasBinAsync.
 * Runs with mocha.
 */
'use strict'

const hasBinAsync = require('../lib/helpers/has_bin_async.js')
const assert = require('assert')
const co = require('co')

describe('has-bin-async', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Has bin async', () => co(function * () {

  }))
})

/* global describe, before, after, it */
