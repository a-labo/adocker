/**
 * Test case for logs.
 * Runs with mocha.
 */
'use strict'

const logs = require('../lib/commands/logs.js')
const assert = require('assert')


describe('logs', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Logs', async () => {

  })
})

/* global describe, before, after, it */
