/**
 * Test case for start.
 * Runs with mocha.
 */
'use strict'

const start = require('../lib/commands/start.js')
const assert = require('assert')
const co = require('co')

describe('start', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Start', () => co(function * () {

  }))
})

/* global describe, before, after, it */
