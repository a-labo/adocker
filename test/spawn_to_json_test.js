/**
 * Test case for spawnToJson.
 * Runs with mocha.
 */
'use strict'

const spawnToJson = require('../lib/helpers/spawn_to_json.js')
const assert = require('assert')
const co = require('co')

describe('spawn-to-json', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Spawn to json', () => co(function * () {

  }))
})

/* global describe, before, after, it */
