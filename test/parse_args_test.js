/**
 * Test case for parseArgs.
 * Runs with mocha.
 */
'use strict'

const parseArgs = require('../lib/helpers/parse_args.js')
const assert = require('assert')
const co = require('co')

describe('parse-args', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Parse args', () => co(function * () {

  }))
})

/* global describe, before, after, it */
