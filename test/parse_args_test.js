/**
 * Test case for parseArgs.
 * Runs with mocha.
 */
'use strict'

const parseArgs = require('../lib/helpers/parse_args.js')
const assert = require('assert')


describe('parse-args', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Parse args', async () => {

  })
})

/* global describe, before, after, it */
