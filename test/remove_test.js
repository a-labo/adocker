/**
 * Test case for remove.
 * Runs with mocha.
 */
'use strict'

const remove = require('../lib/commands/remove.js')
const assert = require('assert')
const co = require('co')

describe('remove', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Remove', () => co(function * () {

  }))
})

/* global describe, before, after, it */
