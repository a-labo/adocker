/**
 * Test case for remove.
 * Runs with mocha.
 */
'use strict'

const remove = require('../lib/commands/remove.js')
const assert = require('assert')


describe('remove', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Remove', async () => {

  })
})

/* global describe, before, after, it */
