/**
 * Test case for docker.
 * Runs with mocha.
 */
'use strict'

const docker = require('../lib/helpers/docker.js')
const assert = require('assert')
const co = require('co')

describe('docker', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Docker', () => co(function * () {
  }))
})

/* global describe, before, after, it */
