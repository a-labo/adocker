/**
 * Test case for spawnInherit.
 * Runs with mocha.
 */
'use strict'

const spawnInherit = require('../lib/helpers/spawn_inherit.js')
const assert = require('assert')


describe('spawn-inherit', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Spawn inherit', async () => {

  })
})

/* global describe, before, after, it */
