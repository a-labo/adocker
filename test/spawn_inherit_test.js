/**
 * Test case for spawnInherit.
 * Runs with mocha.
 */
'use strict'

const spawnInherit = require('../lib/helpers/spawn_inherit.js')
const assert = require('assert')
const co = require('co')

describe('spawn-inherit', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Spawn inherit', () => co(function * () {

  }))
})

/* global describe, before, after, it */
