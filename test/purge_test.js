/**
 * Test case for purge.
 * Runs with mocha.
 */
'use strict'

const purge = require('../lib/commands/purge.js')
const assert = require('assert')


describe('purge', function () {
  this.timeout(3000)

  before(async () => {

  })

  after(async () => {

  })

  it('Purge', async () => {

  })
})

/* global describe, before, after, it */
